# Sentinel Forge — Personal Development dependencies
#
# Core app runs on the Python standard library (tkinter). The packages below
# unlock optional features; install with:  pip install -r requirements.txt
# Every parser/feature is imported try/except — the app still runs if any of
# these are missing, with the matching capability disabled.

# --- Speech-to-text (Whisper voice dictation) ------------------------------
# Whisper-class dictation. faster-whisper downloads its model on first use.
faster-whisper>=1.2.0      # Whisper STT engine (CTranslate2-backed)
sounddevice>=0.5.0         # microphone audio capture
numpy>=1.24                # audio buffers
noisereduce>=3.0.0         # mic noise suppression before transcription

# --- Document loading -----------------------------------------------------
python-docx>=1.1.0         # .docx books
pypdf>=4.0.0               # .pdf books
beautifulsoup4>=4.12.0     # .html / .htm books
openpyxl>=3.1.0            # .xlsx/.xlsm spreadsheets for the assistant (read-only)

# --- Optional niceties ----------------------------------------------------
tkinterdnd2>=0.4.0         # drag-and-drop files into the Library/Study windows
send2trash>=1.8.0          # recoverable deletes from the Library
pyspellchecker>=0.8.0      # offline spelling suggestions for the Voice Memory Spelling Helper

# --- Optional: onboard-assistant voice & accessibility ---------------------
# All guarded by try/except — the app runs fine without them; each only enables
# its own feature. The local AI itself needs Ollama installed separately.
pywin32>=306               # Windows-native SAPI5 TTS for the assistant (lyceum/sapi_tts)
vosk>=0.3.45               # low-latency offline voice commands (lyceum/stt_command; also needs a Vosk model)
# NVDA screen-reader bridge (lyceum/accessibility_bridge) needs no pip package —
# it loads the user's already-installed NVDA controller DLL via ctypes, if present.
